mutation createResponse($response: SymptomQuestionnaireResponseInput!) {
    createSymptomQuestionnaireResponse(response: $response) {
        id
        score {
            value
            color
            title
            description
        }
    }
}
